// Site-wide feature flags.
//
// SHOW_AD_PLACEHOLDERS: while true, dashed "Ad placeholder" boxes render
// in their reserved layout spots (header, sidebar, in-content, footer).
// Keep this false for real visitors until actual ad code is going in —
// flip to true only when testing layout spacing for a new ad slot.
export const SHOW_AD_PLACEHOLDERS = false;
